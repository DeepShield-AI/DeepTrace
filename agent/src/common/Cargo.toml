[package]
name = "mercury-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { workspace = true, optional = true }
num_enum = { version = "0.7.3", default-features = false }

[lib]
path = "src/lib.rs"
